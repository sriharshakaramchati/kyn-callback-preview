{
  "name": "community-network-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "vitest run",
    "server": "node server/main.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs",
    "test:browser": "playwright test",
    "build:staging": "npm run build && node scripts/staging.mjs",
    "pretest:browser": "node tests/browser/build-staging.mjs"
  },
  "dependencies": {
    "@reclaimprotocol/js-sdk": "5.8.2",
    "express": "5.2.1",
    "papaparse": "^5.7.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.5.2",
    "assert": "^2.1.0",
    "esbuild": "^0.25.10",
    "ethers": "^6.15.0",
    "nock": "^14.0.10",
    "process": "^0.11.10",
    "supertest": "^7.1.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
